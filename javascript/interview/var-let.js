/*
  the var and let keywords are both used to declare variables in Javscript.
  However, there are some key differences between the two keywords.

  the var keyword declared a global variable, which means that the
  variable can accessed from anywhere in the code.
*/
